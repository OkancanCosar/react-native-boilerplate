export const Colors = isDark => {
  return {
    bg: isDark ? "#000" : "#fff",
    clr: isDark ? "#fff" : "#000",

    ListBacground: isDark ? "#514949" : "#fff",
    ListItemTextColor: isDark ? "#fff" : "#000",

    xyz: isDark ? "red" : "blue",
  };
};
